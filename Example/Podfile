
use_frameworks!
source 'https://cdn.cocoapods.org/'
install! 'cocoapods', :warn_for_unused_master_specs_repo => false

platform :ios, '10.0'

def gitUrl (name)
   url = 'https://github.com/RokidiOS/'
   return url + name
end

target 'RKIM_Example' do
  pod 'RKIM', :path => '../'
  pod 'RKILogger', '0.2.7'
  pod 'RKIBaseView', :git => gitUrl("RKBaseView")
  pod 'RKIUtils'
  pod 'RKIHandyJSON'
  pod 'RKIMCore', :git => gitUrl("RKIMCore"), :branch => "1.1.2_swift5.6.0"
  pod 'LookinServer', :configurations => ['Debug']
  pod 'DoraemonKit'
  pod 'RKSocket', :git => gitUrl("RKSocket")
  pod 'Moya', '~> 15.0.0'
end


post_install do |installer|
  installer.pods_project.targets.each do |target|
    target.build_configurations.each do |config|
      config.build_settings['ENABLE_BITCODE'] = 'NO'
#      config.build_settings["IPHONEOS_DEPLOYMENT_TARGET"] = "iOS 10.0"
    end
  end
end
